| Function        | Example                   | Output          | Use                                     |
| --------------- | ------------------------- | --------------- | --------------------------------------- |
|  push()         |  arr.push("new")          | Add to end      | Add new item to list (e.g., cart, todo) |
|  pop()          |  arr.pop()                | Remove last     | Remove latest entry                     |
|  shift()        |  arr.shift()              | Remove first    | Queue processing                        |
|  unshift()      |  arr.unshift("start")     | Add to start    | Queue start push                        |
|  slice(1,3)     |  arr.slice(1,3)           | Part of array   | Show limited view                       |
|  splice()       |  arr.splice(1, 1, "new")  | Modify in-place | Replace/delete values                   |
|  includes("x")  |  arr.includes("x")        | true/false      | Check if item exists                    |
|  indexOf("x")   |  arr.indexOf("x")         | Position/index  | Find where an item is                   |
|  join("-")      |  arr.join("-")            | "a-b-c"         | Convert array to string                 |
|  map()          |  arr.map(x => x*2)        | [2,4,6]         | Create new array with changes           |
|  filter()       |  arr.filter(x => x>2)     | [3,4]           | Keep only valid entries                 |
|  reduce()       |  arr.reduce((a,b)=>a+b)   | Sum/total       | Add all items (e.g., price total)       |
|  sort()         |  arr.sort()               | Sorted list     | Alphabetical/numeric order              |
|  reverse()      |  arr.reverse()            | Reversed array  | Flip array                              |
